# fem_tools

A simple Python package for modal analysis of 2-D plane trusses. Assembles global mass and stiffness matrices, solves the generalised eigenvalue problem with support for linear constraints, and animates mode shapes in Jupyter notebooks.

## Installation

```bash
pip install -e .
```